"use strict"

function convert_single_word(word){
  var word;
  var final_word;
  vowels = ['A','I','U','E','O'];

  if word = str.startsWith(vowels[]){
    final_word = word;
  }
  else {
    final_word = word.substr(1) + word.slice(0,1) + 'ay';
  }
  return final_word;
}

function convert_complete_sentence(sentence){

}
//use readline to fix this challenge
const readline = require('readline');
